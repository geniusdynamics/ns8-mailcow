#!/usr/bin/env python3

#
# Copyright (C) 2022 Nethesis S.r.l.
# SPDX-License-Identifier: GPL-3.0-or-later
#

import json
import sys
import agent

# Try to parse the stdin as JSON.
# If parsing fails, output everything to stderr
data = json.load(sys.stdin)

#This is specific to you module, so you need to change it accordingly.
#agent.dump_env()
TZ = data.get("TZ", "${TZ}") 
SKIP_UNBOUND_HEALTHCHECK = data.get("SKIP_UNBOUND_HEALTHCHECK", "${SKIP_UNBOUND_HEALTHCHECK:-n}") 
 
unbound-mailcow = { 
 "TZ" : TZ ,
 "SKIP_UNBOUND_HEALTHCHECK" : SKIP_UNBOUND_HEALTHCHECK ,
} 
agent.write_envfile("unbound-mailcow.env", unbound-mailcow) 
TZ = data.get("TZ", "${TZ}") 
SKIP_CLAMD = data.get("SKIP_CLAMD", "${SKIP_CLAMD:-n}") 
 
clamd-mailcow = { 
 "TZ" : TZ ,
 "SKIP_CLAMD" : SKIP_CLAMD ,
} 
agent.write_envfile("clamd-mailcow.env", clamd-mailcow) 
DBNAME = data.get("DBNAME", "${DBNAME}") 
TZ = data.get("TZ", "${TZ}") 
LOG_LINES = data.get("LOG_LINES", "${LOG_LINES:-9999}") 
MAILCOW_PASS_SCHEME = data.get("MAILCOW_PASS_SCHEME", "${MAILCOW_PASS_SCHEME:-BLF-CRYPT}") 
MASTER = data.get("MASTER", "${MASTER:-y}") 
DBPASS = data.get("DBPASS", "${DBPASS}") 
MAILCOW_HOSTNAME = data.get("MAILCOW_HOSTNAME", "${MAILCOW_HOSTNAME}") 
SOGO_URL_ENCRYPTION_KEY = data.get("SOGO_URL_ENCRYPTION_KEY", "${SOGO_URL_ENCRYPTION_KEY:-SOGoSuperSecret0}") 
SKIP_SOGO = data.get("SKIP_SOGO", "${SKIP_SOGO:-n}") 
REDIS_SLAVEOF_PORT = data.get("REDIS_SLAVEOF_PORT", "${REDIS_SLAVEOF_PORT:-}") 
ACL_ANYONE = data.get("ACL_ANYONE", "${ACL_ANYONE:-disallow}") 
ALLOW_ADMIN_EMAIL_LOGIN = data.get("ALLOW_ADMIN_EMAIL_LOGIN", "${ALLOW_ADMIN_EMAIL_LOGIN:-n}") 
REDISPASS = data.get("REDISPASS", "${REDISPASS}") 
DBUSER = data.get("DBUSER", "${DBUSER}") 
IPV4_NETWORK = data.get("IPV4_NETWORK", "${IPV4_NETWORK:-172.22.1}") 
SOGO_EXPIRE_SESSION = data.get("SOGO_EXPIRE_SESSION", "${SOGO_EXPIRE_SESSION:-480}") 
REDIS_SLAVEOF_IP = data.get("REDIS_SLAVEOF_IP", "${REDIS_SLAVEOF_IP:-}") 
 
sogo-mailcow = { 
 "DBNAME" : DBNAME ,
 "TZ" : TZ ,
 "LOG_LINES" : LOG_LINES ,
 "MAILCOW_PASS_SCHEME" : MAILCOW_PASS_SCHEME ,
 "MASTER" : MASTER ,
 "DBPASS" : DBPASS ,
 "MAILCOW_HOSTNAME" : MAILCOW_HOSTNAME ,
 "SOGO_URL_ENCRYPTION_KEY" : SOGO_URL_ENCRYPTION_KEY ,
 "SKIP_SOGO" : SKIP_SOGO ,
 "REDIS_SLAVEOF_PORT" : REDIS_SLAVEOF_PORT ,
 "ACL_ANYONE" : ACL_ANYONE ,
 "ALLOW_ADMIN_EMAIL_LOGIN" : ALLOW_ADMIN_EMAIL_LOGIN ,
 "REDISPASS" : REDISPASS ,
 "DBUSER" : DBUSER ,
 "IPV4_NETWORK" : IPV4_NETWORK ,
 "SOGO_EXPIRE_SESSION" : SOGO_EXPIRE_SESSION ,
 "REDIS_SLAVEOF_IP" : REDIS_SLAVEOF_IP ,
} 
agent.write_envfile("sogo-mailcow.env", sogo-mailcow) 
ALLOW_ADMIN_EMAIL_LOGIN = data.get("ALLOW_ADMIN_EMAIL_LOGIN", "${ALLOW_ADMIN_EMAIL_LOGIN:-n}") 
MAILDIR_GC_TIME = data.get("MAILDIR_GC_TIME", "${MAILDIR_GC_TIME:-7200}") 
SKIP_FTS = data.get("SKIP_FTS", "${SKIP_FTS:-y}") 
FTS_PROCS = data.get("FTS_PROCS", "${FTS_PROCS:-3}") 
DOVECOT_MASTER_PASS = data.get("DOVECOT_MASTER_PASS", "${DOVECOT_MASTER_PASS:-}") 
LOG_LINES = data.get("LOG_LINES", "${LOG_LINES:-9999}") 
DBPASS = data.get("DBPASS", "${DBPASS}") 
MAILCOW_PASS_SCHEME = data.get("MAILCOW_PASS_SCHEME", "${MAILCOW_PASS_SCHEME:-BLF-CRYPT}") 
ACL_ANYONE = data.get("ACL_ANYONE", "${ACL_ANYONE:-disallow}") 
MAILDIR_SUB = data.get("MAILDIR_SUB", "${MAILDIR_SUB:-}") 
MASTER = data.get("MASTER", "${MASTER:-y}") 
REDIS_SLAVEOF_IP = data.get("REDIS_SLAVEOF_IP", "${REDIS_SLAVEOF_IP:-}") 
FTS_HEAP = data.get("FTS_HEAP", "${FTS_HEAP:-512}") 
REDISPASS = data.get("REDISPASS", "${REDISPASS}") 
MAILCOW_REPLICA_IP = data.get("MAILCOW_REPLICA_IP", "${MAILCOW_REPLICA_IP:-}") 
DBNAME = data.get("DBNAME", "${DBNAME}") 
DBUSER = data.get("DBUSER", "${DBUSER}") 
TZ = data.get("TZ", "${TZ}") 
REDIS_SLAVEOF_PORT = data.get("REDIS_SLAVEOF_PORT", "${REDIS_SLAVEOF_PORT:-}") 
COMPOSE_PROJECT_NAME = data.get("COMPOSE_PROJECT_NAME", "${COMPOSE_PROJECT_NAME:-mailcow-dockerized}") 
DOVECOT_MASTER_USER = data.get("DOVECOT_MASTER_USER", "${DOVECOT_MASTER_USER:-}") 
DOVEADM_REPLICA_PORT = data.get("DOVEADM_REPLICA_PORT", "${DOVEADM_REPLICA_PORT:-}") 
MAILCOW_HOSTNAME = data.get("MAILCOW_HOSTNAME", "${MAILCOW_HOSTNAME}") 
IPV4_NETWORK = data.get("IPV4_NETWORK", "${IPV4_NETWORK:-172.22.1}") 
 
dovecot-mailcow = { 
 "ALLOW_ADMIN_EMAIL_LOGIN" : ALLOW_ADMIN_EMAIL_LOGIN ,
 "MAILDIR_GC_TIME" : MAILDIR_GC_TIME ,
 "SKIP_FTS" : SKIP_FTS ,
 "FTS_PROCS" : FTS_PROCS ,
 "DOVECOT_MASTER_PASS" : DOVECOT_MASTER_PASS ,
 "LOG_LINES" : LOG_LINES ,
 "DBPASS" : DBPASS ,
 "MAILCOW_PASS_SCHEME" : MAILCOW_PASS_SCHEME ,
 "ACL_ANYONE" : ACL_ANYONE ,
 "MAILDIR_SUB" : MAILDIR_SUB ,
 "MASTER" : MASTER ,
 "REDIS_SLAVEOF_IP" : REDIS_SLAVEOF_IP ,
 "FTS_HEAP" : FTS_HEAP ,
 "REDISPASS" : REDISPASS ,
 "MAILCOW_REPLICA_IP" : MAILCOW_REPLICA_IP ,
 "DBNAME" : DBNAME ,
 "DBUSER" : DBUSER ,
 "TZ" : TZ ,
 "REDIS_SLAVEOF_PORT" : REDIS_SLAVEOF_PORT ,
 "COMPOSE_PROJECT_NAME" : COMPOSE_PROJECT_NAME ,
 "DOVECOT_MASTER_USER" : DOVECOT_MASTER_USER ,
 "DOVEADM_REPLICA_PORT" : DOVEADM_REPLICA_PORT ,
 "MAILCOW_HOSTNAME" : MAILCOW_HOSTNAME ,
 "IPV4_NETWORK" : IPV4_NETWORK ,
} 
agent.write_envfile("dovecot-mailcow.env", dovecot-mailcow) 
LOG_LINES = data.get("LOG_LINES", "${LOG_LINES:-9999}") 
TZ = data.get("TZ", "${TZ}") 
REDIS_SLAVEOF_IP = data.get("REDIS_SLAVEOF_IP", "${REDIS_SLAVEOF_IP:-}") 
REDIS_SLAVEOF_PORT = data.get("REDIS_SLAVEOF_PORT", "${REDIS_SLAVEOF_PORT:-}") 
REDISPASS = data.get("REDISPASS", "${REDISPASS}") 
DEV_MODE = data.get("DEV_MODE", "${DEV_MODE:-n}") 
 
postfix-tlspol-mailcow = { 
 "LOG_LINES" : LOG_LINES ,
 "TZ" : TZ ,
 "REDIS_SLAVEOF_IP" : REDIS_SLAVEOF_IP ,
 "REDIS_SLAVEOF_PORT" : REDIS_SLAVEOF_PORT ,
 "REDISPASS" : REDISPASS ,
 "DEV_MODE" : DEV_MODE ,
} 
agent.write_envfile("postfix-tlspol-mailcow.env", postfix-tlspol-mailcow) 
REDISHOST = data.get("REDISHOST", "${REDISHOST:-}") 
NGINX_USE_PROXY_PROTOCOL = data.get("NGINX_USE_PROXY_PROTOCOL", "${NGINX_USE_PROXY_PROTOCOL:-n}") 
TZ = data.get("TZ", "${TZ}") 
SKIP_RSPAMD = data.get("SKIP_RSPAMD", "${SKIP_RSPAMD:-n}") 
TRUSTED_PROXIES = data.get("TRUSTED_PROXIES", "${TRUSTED_PROXIES:-}") 
IPV4_NETWORK = data.get("IPV4_NETWORK", "${IPV4_NETWORK:-172.22.1}") 
SKIP_SOGO = data.get("SKIP_SOGO", "${SKIP_SOGO:-n}") 
HTTP_REDIRECT = data.get("HTTP_REDIRECT", "${HTTP_REDIRECT:-n}") 
HTTP_PORT = data.get("HTTP_PORT", "${HTTP_PORT:-80}") 
ADDITIONAL_SERVER_NAMES = data.get("ADDITIONAL_SERVER_NAMES", "${ADDITIONAL_SERVER_NAMES:-}") 
SOGOHOST = data.get("SOGOHOST", "${SOGOHOST:-}") 
HTTPS_PORT = data.get("HTTPS_PORT", "${HTTPS_PORT:-443}") 
MAILCOW_HOSTNAME = data.get("MAILCOW_HOSTNAME", "${MAILCOW_HOSTNAME}") 
ENABLE_IPV6 = data.get("ENABLE_IPV6", "${ENABLE_IPV6:-true}") 
PHPFPMHOST = data.get("PHPFPMHOST", "${PHPFPMHOST:-}") 
RSPAMDHOST = data.get("RSPAMDHOST", "${RSPAMDHOST:-}") 
 
nginx-mailcow = { 
 "REDISHOST" : REDISHOST ,
 "NGINX_USE_PROXY_PROTOCOL" : NGINX_USE_PROXY_PROTOCOL ,
 "TZ" : TZ ,
 "SKIP_RSPAMD" : SKIP_RSPAMD ,
 "TRUSTED_PROXIES" : TRUSTED_PROXIES ,
 "IPV4_NETWORK" : IPV4_NETWORK ,
 "SKIP_SOGO" : SKIP_SOGO ,
 "HTTP_REDIRECT" : HTTP_REDIRECT ,
 "HTTP_PORT" : HTTP_PORT ,
 "ADDITIONAL_SERVER_NAMES" : ADDITIONAL_SERVER_NAMES ,
 "SOGOHOST" : SOGOHOST ,
 "HTTPS_PORT" : HTTPS_PORT ,
 "MAILCOW_HOSTNAME" : MAILCOW_HOSTNAME ,
 "ENABLE_IPV6" : ENABLE_IPV6 ,
 "PHPFPMHOST" : PHPFPMHOST ,
 "RSPAMDHOST" : RSPAMDHOST ,
} 
agent.write_envfile("nginx-mailcow.env", nginx-mailcow) 
REDIS_SLAVEOF_IP = data.get("REDIS_SLAVEOF_IP", "${REDIS_SLAVEOF_IP:-}") 
REDIS_SLAVEOF_PORT = data.get("REDIS_SLAVEOF_PORT", "${REDIS_SLAVEOF_PORT:-}") 
REDISPASS = data.get("REDISPASS", "${REDISPASS}") 
DBROOT = data.get("DBROOT", "${DBROOT}") 
TZ = data.get("TZ", "${TZ}") 
 
dockerapi-mailcow = { 
 "REDIS_SLAVEOF_IP" : REDIS_SLAVEOF_IP ,
 "REDIS_SLAVEOF_PORT" : REDIS_SLAVEOF_PORT ,
 "REDISPASS" : REDISPASS ,
 "DBROOT" : DBROOT ,
 "TZ" : TZ ,
} 
agent.write_envfile("dockerapi-mailcow.env", dockerapi-mailcow) 
OLEFY_BINDADDRESS = data.get("OLEFY_BINDADDRESS", "0.0.0.0") 
OLEFY_TMPDIR = data.get("OLEFY_TMPDIR", "/tmp") 
OLEFY_PYTHON_PATH = data.get("OLEFY_PYTHON_PATH", "/usr/bin/python3") 
OLEFY_LOGLVL = data.get("OLEFY_LOGLVL", "20") 
OLEFY_MINLENGTH = data.get("OLEFY_MINLENGTH", "500") 
OLEFY_DEL_TMP = data.get("OLEFY_DEL_TMP", "1") 
TZ = data.get("TZ", "${TZ}") 
OLEFY_BINDPORT = data.get("OLEFY_BINDPORT", "10055") 
OLEFY_OLEVBA_PATH = data.get("OLEFY_OLEVBA_PATH", "/usr/bin/olevba") 
SKIP_OLEFY = data.get("SKIP_OLEFY", "${SKIP_OLEFY:-n}") 
 
olefy-mailcow = { 
 "OLEFY_BINDADDRESS" : OLEFY_BINDADDRESS ,
 "OLEFY_TMPDIR" : OLEFY_TMPDIR ,
 "OLEFY_PYTHON_PATH" : OLEFY_PYTHON_PATH ,
 "OLEFY_LOGLVL" : OLEFY_LOGLVL ,
 "OLEFY_MINLENGTH" : OLEFY_MINLENGTH ,
 "OLEFY_DEL_TMP" : OLEFY_DEL_TMP ,
 "TZ" : TZ ,
 "OLEFY_BINDPORT" : OLEFY_BINDPORT ,
 "OLEFY_OLEVBA_PATH" : OLEFY_OLEVBA_PATH ,
 "SKIP_OLEFY" : SKIP_OLEFY ,
} 
agent.write_envfile("olefy-mailcow.env", olefy-mailcow) 
DBPASS = data.get("DBPASS", "${DBPASS}") 
REDIS_SLAVEOF_IP = data.get("REDIS_SLAVEOF_IP", "${REDIS_SLAVEOF_IP:-}") 
REDIS_SLAVEOF_PORT = data.get("REDIS_SLAVEOF_PORT", "${REDIS_SLAVEOF_PORT:-}") 
REDISPASS = data.get("REDISPASS", "${REDISPASS}") 
SPAMHAUS_DQS_KEY = data.get("SPAMHAUS_DQS_KEY", "${SPAMHAUS_DQS_KEY:-}") 
LOG_LINES = data.get("LOG_LINES", "${LOG_LINES:-9999}") 
DBNAME = data.get("DBNAME", "${DBNAME}") 
DBUSER = data.get("DBUSER", "${DBUSER}") 
MAILCOW_HOSTNAME = data.get("MAILCOW_HOSTNAME", "${MAILCOW_HOSTNAME}") 
TZ = data.get("TZ", "${TZ}") 
 
postfix-mailcow = { 
 "DBPASS" : DBPASS ,
 "REDIS_SLAVEOF_IP" : REDIS_SLAVEOF_IP ,
 "REDIS_SLAVEOF_PORT" : REDIS_SLAVEOF_PORT ,
 "REDISPASS" : REDISPASS ,
 "SPAMHAUS_DQS_KEY" : SPAMHAUS_DQS_KEY ,
 "LOG_LINES" : LOG_LINES ,
 "DBNAME" : DBNAME ,
 "DBUSER" : DBUSER ,
 "MAILCOW_HOSTNAME" : MAILCOW_HOSTNAME ,
 "TZ" : TZ ,
} 
agent.write_envfile("postfix-mailcow.env", postfix-mailcow) 
TZ = data.get("TZ", "${TZ}") 
 
memcached-mailcow = { 
 "TZ" : TZ ,
} 
agent.write_envfile("memcached-mailcow.env", memcached-mailcow) 
ADDITIONAL_SAN = data.get("ADDITIONAL_SAN", "${ADDITIONAL_SAN}") 
SKIP_LETS_ENCRYPT = data.get("SKIP_LETS_ENCRYPT", "${SKIP_LETS_ENCRYPT:-n}") 
COMPOSE_PROJECT_NAME = data.get("COMPOSE_PROJECT_NAME", "${COMPOSE_PROJECT_NAME:-mailcow-dockerized}") 
DIRECTORY_URL = data.get("DIRECTORY_URL", "${DIRECTORY_URL:-}") 
SKIP_HTTP_VERIFICATION = data.get("SKIP_HTTP_VERIFICATION", "${SKIP_HTTP_VERIFICATION:-n}") 
REDISPASS = data.get("REDISPASS", "${REDISPASS}") 
SNAT_TO_SOURCE = data.get("SNAT_TO_SOURCE", "${SNAT_TO_SOURCE:-n}") 
LOG_LINES = data.get("LOG_LINES", "${LOG_LINES:-9999}") 
AUTODISCOVER_SAN = data.get("AUTODISCOVER_SAN", "${AUTODISCOVER_SAN:-y}") 
MAILCOW_HOSTNAME = data.get("MAILCOW_HOSTNAME", "${MAILCOW_HOSTNAME}") 
DBPASS = data.get("DBPASS", "${DBPASS}") 
ENABLE_SSL_SNI = data.get("ENABLE_SSL_SNI", "${ENABLE_SSL_SNI:-n}") 
TZ = data.get("TZ", "${TZ}") 
DBNAME = data.get("DBNAME", "${DBNAME}") 
DBUSER = data.get("DBUSER", "${DBUSER}") 
SKIP_IP_CHECK = data.get("SKIP_IP_CHECK", "${SKIP_IP_CHECK:-n}") 
ONLY_MAILCOW_HOSTNAME = data.get("ONLY_MAILCOW_HOSTNAME", "${ONLY_MAILCOW_HOSTNAME:-n}") 
REDIS_SLAVEOF_IP = data.get("REDIS_SLAVEOF_IP", "${REDIS_SLAVEOF_IP:-}") 
REDIS_SLAVEOF_PORT = data.get("REDIS_SLAVEOF_PORT", "${REDIS_SLAVEOF_PORT:-}") 
LE_STAGING = data.get("LE_STAGING", "${LE_STAGING:-n}") 
SNAT6_TO_SOURCE = data.get("SNAT6_TO_SOURCE", "${SNAT6_TO_SOURCE:-n}") 
 
acme-mailcow = { 
 "ADDITIONAL_SAN" : ADDITIONAL_SAN ,
 "SKIP_LETS_ENCRYPT" : SKIP_LETS_ENCRYPT ,
 "COMPOSE_PROJECT_NAME" : COMPOSE_PROJECT_NAME ,
 "DIRECTORY_URL" : DIRECTORY_URL ,
 "SKIP_HTTP_VERIFICATION" : SKIP_HTTP_VERIFICATION ,
 "REDISPASS" : REDISPASS ,
 "SNAT_TO_SOURCE" : SNAT_TO_SOURCE ,
 "LOG_LINES" : LOG_LINES ,
 "AUTODISCOVER_SAN" : AUTODISCOVER_SAN ,
 "MAILCOW_HOSTNAME" : MAILCOW_HOSTNAME ,
 "DBPASS" : DBPASS ,
 "ENABLE_SSL_SNI" : ENABLE_SSL_SNI ,
 "TZ" : TZ ,
 "DBNAME" : DBNAME ,
 "DBUSER" : DBUSER ,
 "SKIP_IP_CHECK" : SKIP_IP_CHECK ,
 "ONLY_MAILCOW_HOSTNAME" : ONLY_MAILCOW_HOSTNAME ,
 "REDIS_SLAVEOF_IP" : REDIS_SLAVEOF_IP ,
 "REDIS_SLAVEOF_PORT" : REDIS_SLAVEOF_PORT ,
 "LE_STAGING" : LE_STAGING ,
 "SNAT6_TO_SOURCE" : SNAT6_TO_SOURCE ,
} 
agent.write_envfile("acme-mailcow.env", acme-mailcow) 
SNAT_TO_SOURCE = data.get("SNAT_TO_SOURCE", "${SNAT_TO_SOURCE:-n}") 
REDIS_SLAVEOF_IP = data.get("REDIS_SLAVEOF_IP", "${REDIS_SLAVEOF_IP:-}") 
REDISPASS = data.get("REDISPASS", "${REDISPASS}") 
MAILCOW_REPLICA_IP = data.get("MAILCOW_REPLICA_IP", "${MAILCOW_REPLICA_IP:-}") 
DISABLE_NETFILTER_ISOLATION_RULE = data.get("DISABLE_NETFILTER_ISOLATION_RULE", "${DISABLE_NETFILTER_ISOLATION_RULE:-n}") 
SNAT6_TO_SOURCE = data.get("SNAT6_TO_SOURCE", "${SNAT6_TO_SOURCE:-n}") 
REDIS_SLAVEOF_PORT = data.get("REDIS_SLAVEOF_PORT", "${REDIS_SLAVEOF_PORT:-}") 
TZ = data.get("TZ", "${TZ}") 
IPV4_NETWORK = data.get("IPV4_NETWORK", "${IPV4_NETWORK:-172.22.1}") 
IPV6_NETWORK = data.get("IPV6_NETWORK", "${IPV6_NETWORK:-fd4d:6169:6c63:6f77::/64}") 
 
netfilter-mailcow = { 
 "SNAT_TO_SOURCE" : SNAT_TO_SOURCE ,
 "REDIS_SLAVEOF_IP" : REDIS_SLAVEOF_IP ,
 "REDISPASS" : REDISPASS ,
 "MAILCOW_REPLICA_IP" : MAILCOW_REPLICA_IP ,
 "DISABLE_NETFILTER_ISOLATION_RULE" : DISABLE_NETFILTER_ISOLATION_RULE ,
 "SNAT6_TO_SOURCE" : SNAT6_TO_SOURCE ,
 "REDIS_SLAVEOF_PORT" : REDIS_SLAVEOF_PORT ,
 "TZ" : TZ ,
 "IPV4_NETWORK" : IPV4_NETWORK ,
 "IPV6_NETWORK" : IPV6_NETWORK ,
} 
agent.write_envfile("netfilter-mailcow.env", netfilter-mailcow) 
TZ = data.get("TZ", "${TZ}") 
SKIP_LETS_ENCRYPT = data.get("SKIP_LETS_ENCRYPT", "${SKIP_LETS_ENCRYPT:-n}") 
HTTPS_PORT = data.get("HTTPS_PORT", "${HTTPS_PORT:-443}") 
MYSQL_REPLICATION_THRESHOLD = data.get("MYSQL_REPLICATION_THRESHOLD", "${MYSQL_REPLICATION_THRESHOLD:-1}") 
RSPAMD_THRESHOLD = data.get("RSPAMD_THRESHOLD", "${RSPAMD_THRESHOLD:-5}") 
MAILQ_CRIT = data.get("MAILQ_CRIT", "${MAILQ_CRIT:-30}") 
DBUSER = data.get("DBUSER", "${DBUSER}") 
IP_BY_DOCKER_API = data.get("IP_BY_DOCKER_API", "${IP_BY_DOCKER_API:-0}") 
CHECK_UNBOUND = data.get("CHECK_UNBOUND", "${CHECK_UNBOUND:-1}") 
POSTFIX_TLSPOL_THRESHOLD = data.get("POSTFIX_TLSPOL_THRESHOLD", "${POSTFIX_TLSPOL_THRESHOLD:-8}") 
RATELIMIT_THRESHOLD = data.get("RATELIMIT_THRESHOLD", "${RATELIMIT_THRESHOLD:-1}") 
WATCHDOG_NOTIFY_BAN = data.get("WATCHDOG_NOTIFY_BAN", "${WATCHDOG_NOTIFY_BAN:-y}") 
COMPOSE_PROJECT_NAME = data.get("COMPOSE_PROJECT_NAME", "${COMPOSE_PROJECT_NAME:-mailcow-dockerized}") 
SKIP_SOGO = data.get("SKIP_SOGO", "${SKIP_SOGO:-n}") 
UNBOUND_THRESHOLD = data.get("UNBOUND_THRESHOLD", "${UNBOUND_THRESHOLD:-5}") 
MYSQL_THRESHOLD = data.get("MYSQL_THRESHOLD", "${MYSQL_THRESHOLD:-5}") 
CLAMD_THRESHOLD = data.get("CLAMD_THRESHOLD", "${CLAMD_THRESHOLD:-15}") 
DOVECOT_THRESHOLD = data.get("DOVECOT_THRESHOLD", "${DOVECOT_THRESHOLD:-12}") 
DBNAME = data.get("DBNAME", "${DBNAME}") 
WATCHDOG_SUBJECT = data.get("WATCHDOG_SUBJECT", "${WATCHDOG_SUBJECT:-Watchdog ALERT}") 
WATCHDOG_NOTIFY_WEBHOOK_BODY = data.get("WATCHDOG_NOTIFY_WEBHOOK_BODY", "${WATCHDOG_NOTIFY_WEBHOOK_BODY:-}") 
WATCHDOG_MYSQL_REPLICATION_CHECKS = data.get("WATCHDOG_MYSQL_REPLICATION_CHECKS", "${WATCHDOG_MYSQL_REPLICATION_CHECKS:-n}") 
WATCHDOG_VERBOSE = data.get("WATCHDOG_VERBOSE", "${WATCHDOG_VERBOSE:-n}") 
IPV4_NETWORK = data.get("IPV4_NETWORK", "${IPV4_NETWORK:-172.22.1}") 
SKIP_OLEFY = data.get("SKIP_OLEFY", "${SKIP_OLEFY:-n}") 
REDIS_SLAVEOF_PORT = data.get("REDIS_SLAVEOF_PORT", "${REDIS_SLAVEOF_PORT:-}") 
LOG_LINES = data.get("LOG_LINES", "${LOG_LINES:-9999}") 
WATCHDOG_NOTIFY_START = data.get("WATCHDOG_NOTIFY_START", "${WATCHDOG_NOTIFY_START:-y}") 
REDIS_SLAVEOF_IP = data.get("REDIS_SLAVEOF_IP", "${REDIS_SLAVEOF_IP:-}") 
REDISPASS = data.get("REDISPASS", "${REDISPASS}") 
EXTERNAL_CHECKS_THRESHOLD = data.get("EXTERNAL_CHECKS_THRESHOLD", "${EXTERNAL_CHECKS_THRESHOLD:-1}") 
SOGO_THRESHOLD = data.get("SOGO_THRESHOLD", "${SOGO_THRESHOLD:-3}") 
POSTFIX_THRESHOLD = data.get("POSTFIX_THRESHOLD", "${POSTFIX_THRESHOLD:-8}") 
DOVECOT_REPL_THRESHOLD = data.get("DOVECOT_REPL_THRESHOLD", "${DOVECOT_REPL_THRESHOLD:-20}") 
WATCHDOG_EXTERNAL_CHECKS = data.get("WATCHDOG_EXTERNAL_CHECKS", "${WATCHDOG_EXTERNAL_CHECKS:-n}") 
MAILCOW_HOSTNAME = data.get("MAILCOW_HOSTNAME", "${MAILCOW_HOSTNAME}") 
FAIL2BAN_THRESHOLD = data.get("FAIL2BAN_THRESHOLD", "${FAIL2BAN_THRESHOLD:-1}") 
ACME_THRESHOLD = data.get("ACME_THRESHOLD", "${ACME_THRESHOLD:-1}") 
DEV_MODE = data.get("DEV_MODE", "${DEV_MODE:-n}") 
DBPASS = data.get("DBPASS", "${DBPASS}") 
WATCHDOG_NOTIFY_EMAIL = data.get("WATCHDOG_NOTIFY_EMAIL", "${WATCHDOG_NOTIFY_EMAIL:-}") 
SKIP_CLAMD = data.get("SKIP_CLAMD", "${SKIP_CLAMD:-n}") 
NGINX_THRESHOLD = data.get("NGINX_THRESHOLD", "${NGINX_THRESHOLD:-5}") 
PHPFPM_THRESHOLD = data.get("PHPFPM_THRESHOLD", "${PHPFPM_THRESHOLD:-5}") 
OLEFY_THRESHOLD = data.get("OLEFY_THRESHOLD", "${OLEFY_THRESHOLD:-5}") 
MAILQ_THRESHOLD = data.get("MAILQ_THRESHOLD", "${MAILQ_THRESHOLD:-20}") 
IPV6_NETWORK = data.get("IPV6_NETWORK", "${IPV6_NETWORK:-fd4d:6169:6c63:6f77::/64}") 
DBROOT = data.get("DBROOT", "${DBROOT}") 
USE_WATCHDOG = data.get("USE_WATCHDOG", "${USE_WATCHDOG:-n}") 
WATCHDOG_NOTIFY_WEBHOOK = data.get("WATCHDOG_NOTIFY_WEBHOOK", "${WATCHDOG_NOTIFY_WEBHOOK:-}") 
REDIS_THRESHOLD = data.get("REDIS_THRESHOLD", "${REDIS_THRESHOLD:-5}") 
 
watchdog-mailcow = { 
 "TZ" : TZ ,
 "SKIP_LETS_ENCRYPT" : SKIP_LETS_ENCRYPT ,
 "HTTPS_PORT" : HTTPS_PORT ,
 "MYSQL_REPLICATION_THRESHOLD" : MYSQL_REPLICATION_THRESHOLD ,
 "RSPAMD_THRESHOLD" : RSPAMD_THRESHOLD ,
 "MAILQ_CRIT" : MAILQ_CRIT ,
 "DBUSER" : DBUSER ,
 "IP_BY_DOCKER_API" : IP_BY_DOCKER_API ,
 "CHECK_UNBOUND" : CHECK_UNBOUND ,
 "POSTFIX_TLSPOL_THRESHOLD" : POSTFIX_TLSPOL_THRESHOLD ,
 "RATELIMIT_THRESHOLD" : RATELIMIT_THRESHOLD ,
 "WATCHDOG_NOTIFY_BAN" : WATCHDOG_NOTIFY_BAN ,
 "COMPOSE_PROJECT_NAME" : COMPOSE_PROJECT_NAME ,
 "SKIP_SOGO" : SKIP_SOGO ,
 "UNBOUND_THRESHOLD" : UNBOUND_THRESHOLD ,
 "MYSQL_THRESHOLD" : MYSQL_THRESHOLD ,
 "CLAMD_THRESHOLD" : CLAMD_THRESHOLD ,
 "DOVECOT_THRESHOLD" : DOVECOT_THRESHOLD ,
 "DBNAME" : DBNAME ,
 "WATCHDOG_SUBJECT" : WATCHDOG_SUBJECT ,
 "WATCHDOG_NOTIFY_WEBHOOK_BODY" : WATCHDOG_NOTIFY_WEBHOOK_BODY ,
 "WATCHDOG_MYSQL_REPLICATION_CHECKS" : WATCHDOG_MYSQL_REPLICATION_CHECKS ,
 "WATCHDOG_VERBOSE" : WATCHDOG_VERBOSE ,
 "IPV4_NETWORK" : IPV4_NETWORK ,
 "SKIP_OLEFY" : SKIP_OLEFY ,
 "REDIS_SLAVEOF_PORT" : REDIS_SLAVEOF_PORT ,
 "LOG_LINES" : LOG_LINES ,
 "WATCHDOG_NOTIFY_START" : WATCHDOG_NOTIFY_START ,
 "REDIS_SLAVEOF_IP" : REDIS_SLAVEOF_IP ,
 "REDISPASS" : REDISPASS ,
 "EXTERNAL_CHECKS_THRESHOLD" : EXTERNAL_CHECKS_THRESHOLD ,
 "SOGO_THRESHOLD" : SOGO_THRESHOLD ,
 "POSTFIX_THRESHOLD" : POSTFIX_THRESHOLD ,
 "DOVECOT_REPL_THRESHOLD" : DOVECOT_REPL_THRESHOLD ,
 "WATCHDOG_EXTERNAL_CHECKS" : WATCHDOG_EXTERNAL_CHECKS ,
 "MAILCOW_HOSTNAME" : MAILCOW_HOSTNAME ,
 "FAIL2BAN_THRESHOLD" : FAIL2BAN_THRESHOLD ,
 "ACME_THRESHOLD" : ACME_THRESHOLD ,
 "DEV_MODE" : DEV_MODE ,
 "DBPASS" : DBPASS ,
 "WATCHDOG_NOTIFY_EMAIL" : WATCHDOG_NOTIFY_EMAIL ,
 "SKIP_CLAMD" : SKIP_CLAMD ,
 "NGINX_THRESHOLD" : NGINX_THRESHOLD ,
 "PHPFPM_THRESHOLD" : PHPFPM_THRESHOLD ,
 "OLEFY_THRESHOLD" : OLEFY_THRESHOLD ,
 "MAILQ_THRESHOLD" : MAILQ_THRESHOLD ,
 "IPV6_NETWORK" : IPV6_NETWORK ,
 "DBROOT" : DBROOT ,
 "USE_WATCHDOG" : USE_WATCHDOG ,
 "WATCHDOG_NOTIFY_WEBHOOK" : WATCHDOG_NOTIFY_WEBHOOK ,
 "REDIS_THRESHOLD" : REDIS_THRESHOLD ,
} 
agent.write_envfile("watchdog-mailcow.env", watchdog-mailcow) 
TZ = data.get("TZ", "${TZ}") 
REDISPASS = data.get("REDISPASS", "${REDISPASS}") 
REDISMASTERPASS = data.get("REDISMASTERPASS", "${REDISMASTERPASS:-}") 
 
redis-mailcow = { 
 "TZ" : TZ ,
 "REDISPASS" : REDISPASS ,
 "REDISMASTERPASS" : REDISMASTERPASS ,
} 
agent.write_envfile("redis-mailcow.env", redis-mailcow) 
TZ = data.get("TZ", "${TZ}") 
IPV4_NETWORK = data.get("IPV4_NETWORK", "${IPV4_NETWORK:-172.22.1}") 
IPV6_NETWORK = data.get("IPV6_NETWORK", "${IPV6_NETWORK:-fd4d:6169:6c63:6f77::/64}") 
REDIS_SLAVEOF_IP = data.get("REDIS_SLAVEOF_IP", "${REDIS_SLAVEOF_IP:-}") 
REDIS_SLAVEOF_PORT = data.get("REDIS_SLAVEOF_PORT", "${REDIS_SLAVEOF_PORT:-}") 
REDISPASS = data.get("REDISPASS", "${REDISPASS}") 
SPAMHAUS_DQS_KEY = data.get("SPAMHAUS_DQS_KEY", "${SPAMHAUS_DQS_KEY:-}") 
 
rspamd-mailcow = { 
 "TZ" : TZ ,
 "IPV4_NETWORK" : IPV4_NETWORK ,
 "IPV6_NETWORK" : IPV6_NETWORK ,
 "REDIS_SLAVEOF_IP" : REDIS_SLAVEOF_IP ,
 "REDIS_SLAVEOF_PORT" : REDIS_SLAVEOF_PORT ,
 "REDISPASS" : REDISPASS ,
 "SPAMHAUS_DQS_KEY" : SPAMHAUS_DQS_KEY ,
} 
agent.write_envfile("rspamd-mailcow.env", rspamd-mailcow) 
TZ = data.get("TZ", "${TZ}") 
COMPOSE_PROJECT_NAME = data.get("COMPOSE_PROJECT_NAME", "${COMPOSE_PROJECT_NAME}") 
 
ofelia-mailcow = { 
 "TZ" : TZ ,
 "COMPOSE_PROJECT_NAME" : COMPOSE_PROJECT_NAME ,
} 
agent.write_envfile("ofelia-mailcow.env", ofelia-mailcow) 
API_ALLOW_FROM = data.get("API_ALLOW_FROM", "${API_ALLOW_FROM:-invalid}") 
DBNAME = data.get("DBNAME", "${DBNAME}") 
DBPASS = data.get("DBPASS", "${DBPASS}") 
MAILCOW_PASS_SCHEME = data.get("MAILCOW_PASS_SCHEME", "${MAILCOW_PASS_SCHEME:-BLF-CRYPT}") 
SIEVE_PORT = data.get("SIEVE_PORT", "${SIEVE_PORT:-4190}") 
SKIP_OLEFY = data.get("SKIP_OLEFY", "${SKIP_OLEFY:-n}") 
TZ = data.get("TZ", "${TZ}") 
CLUSTERMODE = data.get("CLUSTERMODE", "${CLUSTERMODE:-}") 
REDIS_SLAVEOF_PORT = data.get("REDIS_SLAVEOF_PORT", "${REDIS_SLAVEOF_PORT:-}") 
IMAP_PORT = data.get("IMAP_PORT", "${IMAP_PORT:-143}") 
SUBMISSION_PORT = data.get("SUBMISSION_PORT", "${SUBMISSION_PORT:-587}") 
SMTPS_PORT = data.get("SMTPS_PORT", "${SMTPS_PORT:-465}") 
SMTP_PORT = data.get("SMTP_PORT", "${SMTP_PORT:-25}") 
SKIP_SOGO = data.get("SKIP_SOGO", "${SKIP_SOGO:-n}") 
REDIS_SLAVEOF_IP = data.get("REDIS_SLAVEOF_IP", "${REDIS_SLAVEOF_IP:-}") 
REDISPASS = data.get("REDISPASS", "${REDISPASS}") 
WEBAUTHN_ONLY_TRUSTED_VENDORS = data.get("WEBAUTHN_ONLY_TRUSTED_VENDORS", "${WEBAUTHN_ONLY_TRUSTED_VENDORS:-n}") 
COMPOSE_PROJECT_NAME = data.get("COMPOSE_PROJECT_NAME", "${COMPOSE_PROJECT_NAME:-mailcow-dockerized}") 
MAILCOW_HOSTNAME = data.get("MAILCOW_HOSTNAME", "${MAILCOW_HOSTNAME}") 
IMAPS_PORT = data.get("IMAPS_PORT", "${IMAPS_PORT:-993}") 
API_KEY_READ_ONLY = data.get("API_KEY_READ_ONLY", "${API_KEY_READ_ONLY:-invalid}") 
SKIP_CLAMD = data.get("SKIP_CLAMD", "${SKIP_CLAMD:-n}") 
ALLOW_ADMIN_EMAIL_LOGIN = data.get("ALLOW_ADMIN_EMAIL_LOGIN", "${ALLOW_ADMIN_EMAIL_LOGIN:-n}") 
MASTER = data.get("MASTER", "${MASTER:-y}") 
DEMO_MODE = data.get("DEMO_MODE", "${DEMO_MODE:-n}") 
LOG_LINES = data.get("LOG_LINES", "${LOG_LINES:-9999}") 
POP_PORT = data.get("POP_PORT", "${POP_PORT:-110}") 
POPS_PORT = data.get("POPS_PORT", "${POPS_PORT:-995}") 
IPV4_NETWORK = data.get("IPV4_NETWORK", "${IPV4_NETWORK:-172.22.1}") 
API_KEY = data.get("API_KEY", "${API_KEY:-invalid}") 
SKIP_FTS = data.get("SKIP_FTS", "${SKIP_FTS:-y}") 
DEV_MODE = data.get("DEV_MODE", "${DEV_MODE:-n}") 
ADDITIONAL_SERVER_NAMES = data.get("ADDITIONAL_SERVER_NAMES", "${ADDITIONAL_SERVER_NAMES:-}") 
DBUSER = data.get("DBUSER", "${DBUSER}") 
IPV6_NETWORK = data.get("IPV6_NETWORK", "${IPV6_NETWORK:-fd4d:6169:6c63:6f77::/64}") 
 
php-fpm-mailcow = { 
 "API_ALLOW_FROM" : API_ALLOW_FROM ,
 "DBNAME" : DBNAME ,
 "DBPASS" : DBPASS ,
 "MAILCOW_PASS_SCHEME" : MAILCOW_PASS_SCHEME ,
 "SIEVE_PORT" : SIEVE_PORT ,
 "SKIP_OLEFY" : SKIP_OLEFY ,
 "TZ" : TZ ,
 "CLUSTERMODE" : CLUSTERMODE ,
 "REDIS_SLAVEOF_PORT" : REDIS_SLAVEOF_PORT ,
 "IMAP_PORT" : IMAP_PORT ,
 "SUBMISSION_PORT" : SUBMISSION_PORT ,
 "SMTPS_PORT" : SMTPS_PORT ,
 "SMTP_PORT" : SMTP_PORT ,
 "SKIP_SOGO" : SKIP_SOGO ,
 "REDIS_SLAVEOF_IP" : REDIS_SLAVEOF_IP ,
 "REDISPASS" : REDISPASS ,
 "WEBAUTHN_ONLY_TRUSTED_VENDORS" : WEBAUTHN_ONLY_TRUSTED_VENDORS ,
 "COMPOSE_PROJECT_NAME" : COMPOSE_PROJECT_NAME ,
 "MAILCOW_HOSTNAME" : MAILCOW_HOSTNAME ,
 "IMAPS_PORT" : IMAPS_PORT ,
 "API_KEY_READ_ONLY" : API_KEY_READ_ONLY ,
 "SKIP_CLAMD" : SKIP_CLAMD ,
 "ALLOW_ADMIN_EMAIL_LOGIN" : ALLOW_ADMIN_EMAIL_LOGIN ,
 "MASTER" : MASTER ,
 "DEMO_MODE" : DEMO_MODE ,
 "LOG_LINES" : LOG_LINES ,
 "POP_PORT" : POP_PORT ,
 "POPS_PORT" : POPS_PORT ,
 "IPV4_NETWORK" : IPV4_NETWORK ,
 "API_KEY" : API_KEY ,
 "SKIP_FTS" : SKIP_FTS ,
 "DEV_MODE" : DEV_MODE ,
 "ADDITIONAL_SERVER_NAMES" : ADDITIONAL_SERVER_NAMES ,
 "DBUSER" : DBUSER ,
 "IPV6_NETWORK" : IPV6_NETWORK ,
} 
agent.write_envfile("php-fpm-mailcow.env", php-fpm-mailcow) 
